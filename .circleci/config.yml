version: 2
jobs:
  build:
    working_directory: ~/action-postgres-setup
    docker:
      - image: circleci/postgres:11-alpine
        environment:
          POSTGRES_USER: postgres
          POSTGRES_DB: postgres
    steps:
      - run:
          name: Database Setup
          command: |
            pgbench -U postgres -i -h 127.0.0.1 -p 5432 -d postgres
            pgbench -U postgres -h 127.0.0.1 -c 10 -T 30 -S -n
